-e ../../libs/py
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.33
httpx>=0.27,<0.28
# LangGraph + langchain-core pinned to exact versions per global §16
# (reproducible builds). Drift permits subtle agent-behaviour change
# without code review. Bumps are deliberate one-line PRs with a Phase-N
# note in the commit and locked by drill_langgraph_pin.
langgraph==1.1.10
langchain-core==1.3.2
pydantic>=2.7,<3
pytest>=8
pytest-asyncio>=0.23
