apiVersion: v1
kind: Service
metadata:
  name: mcp-aws
  namespace: documind
  labels:
    app: mcp-aws
    app.kubernetes.io/name: mcp-aws
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: documind
    documind.tool/namespace: aws
    documind.tool/category: mcp
spec:
  selector:
    app: mcp-aws
  ports:
    - port: 8126
      targetPort: 8126
      # `name: http` + appProtocol HTTP teaches Istio + Kiali to render
      # this as L7 traffic (not opaque TCP) → service-graph + RPS gauges.
      name: http
      appProtocol: http
      protocol: TCP
---
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: mcp-aws-tel
  namespace: documind
spec:
  selector:
    matchLabels:
      app: mcp-aws
  metrics:
    - providers:
        - name: prometheus
      overrides:
        - match:
            metric: REQUEST_COUNT
            mode: CLIENT_AND_SERVER
          tagOverrides:
            documind_tool_namespace:
              value: "'aws'"
            documind_tool_category:
              value: "'mcp'"
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 100
  accessLogging:
    - providers:
        - name: envoy
