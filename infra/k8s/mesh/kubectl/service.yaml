apiVersion: v1
kind: Service
metadata:
  name: mcp-kubectl
  namespace: documind
  labels:
    app: mcp-kubectl
    documind.tool/namespace: kubectl
spec:
  selector:
    app: mcp-kubectl
  ports:
    - port: 8122
      targetPort: 8122
      name: http
      protocol: TCP
