apiVersion: v1
kind: Service
metadata:
  name: mcp-csv-ingest
  namespace: documind
  labels:
    app: mcp-csv-ingest
    app.kubernetes.io/name: mcp-csv-ingest
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: documind
    documind.tool/namespace: csv_ingest
    documind.tool/category: mcp
spec:
  selector:
    app: mcp-csv-ingest
  ports:
    - port: 8095
      targetPort: 8095
      # `name: http` + appProtocol HTTP teaches Istio + Kiali to render
      # this as L7 traffic (not opaque TCP) → service-graph + RPS gauges.
      name: http
      appProtocol: http
      protocol: TCP
---
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: mcp-csv-ingest-tel
  namespace: documind
spec:
  selector:
    matchLabels:
      app: mcp-csv-ingest
  metrics:
    - providers:
        - name: prometheus
      overrides:
        - match:
            metric: REQUEST_COUNT
            mode: CLIENT_AND_SERVER
          tagOverrides:
            documind_tool_namespace:
              value: "'csv_ingest'"
            documind_tool_category:
              value: "'mcp'"
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 100
  accessLogging:
    - providers:
        - name: envoy
