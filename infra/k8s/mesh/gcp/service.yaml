apiVersion: v1
kind: Service
metadata:
  name: mcp-gcp
  namespace: documind
  labels:
    app: mcp-gcp
    documind.tool/namespace: gcp
spec:
  selector:
    app: mcp-gcp
  ports:
    - port: 8127
      targetPort: 8127
      name: http
      protocol: TCP
