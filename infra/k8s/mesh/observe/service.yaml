apiVersion: v1
kind: Service
metadata:
  name: mcp-observe
  namespace: documind
  labels:
    app: mcp-observe
    documind.tool/namespace: observe
spec:
  selector:
    app: mcp-observe
  ports:
    - port: 8137
      targetPort: 8137
      name: http
      protocol: TCP
