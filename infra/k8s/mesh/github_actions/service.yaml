apiVersion: v1
kind: Service
metadata:
  name: mcp-github-actions
  namespace: documind
  labels:
    app: mcp-github-actions
    app.kubernetes.io/name: mcp-github-actions
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: documind
    documind.tool/namespace: github_actions
    documind.tool/category: mcp
spec:
  selector:
    app: mcp-github-actions
  ports:
    - port: 8120
      targetPort: 8120
      # `name: http` + appProtocol HTTP teaches Istio + Kiali to render
      # this as L7 traffic (not opaque TCP) → service-graph + RPS gauges.
      name: http
      appProtocol: http
      protocol: TCP
---
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: mcp-github-actions-tel
  namespace: documind
spec:
  selector:
    matchLabels:
      app: mcp-github-actions
  metrics:
    - providers:
        - name: prometheus
      overrides:
        - match:
            metric: REQUEST_COUNT
            mode: CLIENT_AND_SERVER
          tagOverrides:
            documind_tool_namespace:
              value: "'github_actions'"
            documind_tool_category:
              value: "'mcp'"
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 100
  accessLogging:
    - providers:
        - name: envoy
