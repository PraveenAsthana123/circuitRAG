apiVersion: v1
kind: Service
metadata:
  name: mcp-github-actions
  namespace: documind
  labels:
    app: mcp-github-actions
    documind.tool/namespace: github_actions
spec:
  selector:
    app: mcp-github-actions
  ports:
    - port: 8120
      targetPort: 8120
      name: http
      protocol: TCP
