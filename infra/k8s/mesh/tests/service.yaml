apiVersion: v1
kind: Service
metadata:
  name: mcp-tests
  namespace: documind
  labels:
    app: mcp-tests
    documind.tool/namespace: tests
spec:
  selector:
    app: mcp-tests
  ports:
    - port: 8135
      targetPort: 8135
      name: http
      protocol: TCP
