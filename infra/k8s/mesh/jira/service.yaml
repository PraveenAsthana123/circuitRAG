apiVersion: v1
kind: Service
metadata:
  name: mcp-jira
  namespace: documind
  labels:
    app: mcp-jira
    documind.tool/namespace: jira
spec:
  selector:
    app: mcp-jira
  ports:
    - port: 8097
      targetPort: 8097
      name: http
      protocol: TCP
