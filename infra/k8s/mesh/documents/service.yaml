apiVersion: v1
kind: Service
metadata:
  name: mcp-documents
  namespace: documind
  labels:
    app: mcp-documents
    documind.tool/namespace: documents
spec:
  selector:
    app: mcp-documents
  ports:
    - port: 8094
      targetPort: 8094
      name: http
      protocol: TCP
