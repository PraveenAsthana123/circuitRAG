apiVersion: v1
kind: Service
metadata:
  name: mcp-documents
  namespace: documind
  labels:
    app: mcp-documents
    app.kubernetes.io/name: mcp-documents
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: documind
    documind.tool/namespace: documents
    documind.tool/category: mcp
spec:
  selector:
    app: mcp-documents
  ports:
    - port: 8094
      targetPort: 8094
      # `name: http` + appProtocol HTTP teaches Istio + Kiali to render
      # this as L7 traffic (not opaque TCP) → service-graph + RPS gauges.
      name: http
      appProtocol: http
      protocol: TCP
---
apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: mcp-documents-tel
  namespace: documind
spec:
  selector:
    matchLabels:
      app: mcp-documents
  metrics:
    - providers:
        - name: prometheus
      overrides:
        - match:
            metric: REQUEST_COUNT
            mode: CLIENT_AND_SERVER
          tagOverrides:
            documind_tool_namespace:
              value: "'documents'"
            documind_tool_category:
              value: "'mcp'"
  tracing:
    - providers:
        - name: jaeger
      randomSamplingPercentage: 100
  accessLogging:
    - providers:
        - name: envoy
