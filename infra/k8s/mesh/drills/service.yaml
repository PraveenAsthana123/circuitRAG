apiVersion: v1
kind: Service
metadata:
  name: mcp-drills
  namespace: documind
  labels:
    app: mcp-drills
    documind.tool/namespace: drills
spec:
  selector:
    app: mcp-drills
  ports:
    - port: 8092
      targetPort: 8092
      name: http
      protocol: TCP
